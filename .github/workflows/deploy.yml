name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm build

      # GitHub Pages serves a plain 404 for any path with no matching static
      # file - only known prerendered routes have one. Serving index.html
      # instead boots the app, whose own router/locale.ts catch-all then
      # redirects to / or /pt client-side.
      - run: cp dist/index.html dist/404.html

      - name: Publish to rafa98neves.github.io
        uses: peaceiris/actions-gh-pages@v4
        with:
          deploy_key: ${{ secrets.PAGES_DEPLOY_KEY }}
          external_repository: rafa98neves/rafa98neves.github.io
          publish_branch: main
          publish_dir: ./dist
          user_name: "github-actions[bot]"
          user_email: "github-actions[bot]@users.noreply.github.com"
